import { PolicyFormatPipe } from './policy-format.pipe';

describe('PolicyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PolicyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
