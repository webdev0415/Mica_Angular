import { GroupSortPipe } from './group-sort.pipe';

describe('GroupSortPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupSortPipe();
    expect(pipe).toBeTruthy();
  });
});
